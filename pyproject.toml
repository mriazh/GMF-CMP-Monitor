[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -m 'not live'"
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "live: Live end-to-end integration tests requiring network and credentials",
]
